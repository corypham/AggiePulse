export function formatDistance(distance: number): string {
  return distance.toFixed(1);
}

export function formatOpenUntil(hours: string): string {
  // Format the hours string (e.g., "10:45 PM")
  return hours;
} 