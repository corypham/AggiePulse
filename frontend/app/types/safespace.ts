export interface SafeSpaceData {
  mainBuilding: {
    count: number;
    capacity: number;
    percentage: number;
  };
  studyRoom: {
    count: number;
    capacity: number;
    percentage: number;
  };
} 